services:
  - type: web
    name: javalearn-backend
    env: node
    buildCommand: |
      apt-get update
      apt-get install -y openjdk-17-jdk
      npm install
    startCommand: node index.js